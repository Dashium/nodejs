{
    "name": "NodeJS",
    "commands": [
        {"name": "update", "cmd": "apt-get update && apt-get install -y curl wget"},
        {"name": "add source", "cmd": "curl -sL https://deb.nodesource.com/setup_lts.x | sudo -E bash -"},
        {"name": "install nodejs", "cmd": "sudo apt install nodejs -y"}
    ]
}